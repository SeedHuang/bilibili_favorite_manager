# Plan A · 批次任务 UX 基建 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Spec:** `docs/superpowers/specs/2026-09-20-批次任务UX设计.md` — **先读它**,本文与它冲突以它为准。

**Goal:** 搭好「任务三件套」的全部共享基建:polls 配置(repo+路由)、useTaskProgress hook、TaskLogDrawer 通用抽屉、ModelManager→TaskSettings 合一表。**不动任何既有任务的运行逻辑。**

**Architecture:** settings 表多 key 存配置(缺 key 代码兜底);后端 GET/PUT /api/settings/polls;前端薄轮询 hook + 泛型日志抽屉壳;设置页按任务行配置轮询/批次/模型三件套。

**Tech Stack:** better-sqlite3、Fastify(vitest inject 测试)、React + antd 5(umijs/max)。

## Global Constraints

- **git 写操作禁止**:不 commit、不建分支、不 git mv。任务终点 = 工作区改好 + 测试过;重命名文件用「新建 + rm 旧文件」实现。提交由用户完成。
- server 测试:`cd server && npx vitest run`(本机无 pnpm);门禁含 `npx tsc --noEmit`。
- web 门禁:`cd web && npx tsc --noEmit` 0 错;`npx max build` 含 "Compiled successfully"(退出码恒 1 是既有 esbuild 问题,忽略,别修)。
- 注释:中文、讲为什么,密度参照 `server/src/db/schema.ts`。
- 白名单与合法值(spec §3):taskType ∈ {'tag','tagcheck','proposals'};intervalMs ∈ {1000,2000,3000,5000,10000};batch 预设 {1,2,5,10,20,30,40,50} 或 1~500 整数;`proposals` 无 batch(始终 null,PUT 忽略 batch 字段)。

---

### Task 1: polls repo(读/写/列 + 兜底)

**Files:**
- Modify: `server/src/db/repo/state.ts`(末尾追加)
- Test: `server/src/db/repo/state.test.ts`(追加 describe)

**Interfaces(后续任务依赖,签名逐字):**
- `type PollConfig = { intervalMs: number; batch: number | null }`
- `const POLL_TASKS = ['tag', 'tagcheck', 'proposals'] as const; type PollTask = (typeof POLL_TASKS)[number]`
- `readPoll(db, taskType: string): PollConfig` — 缺 key/非法值兜底默认
- `writePoll(db, taskType: string, cfg: { intervalMs: number; batch?: number | null }): void` — repo 层**不校验**,校验在路由层
- `listPolls(db): Record<string, PollConfig>` — 白名单全量
- 默认表:`tag → {2000, 16}`(批上限)、`tagcheck → {3000, 200}`(定长)、`proposals → {3000, null}`
- 合法值常量(路由层也要用,导出):`POLL_INTERVALS = [1000,2000,3000,5000,10000]`、`POLL_BATCHES = [1,2,5,10,20,30,40,50]`

- [ ] **Step 1: 写失败测试**(追加到 state.test.ts;该文件已 import openDb/setSetting)

```ts
import { readPoll, writePoll, listPolls } from './state.js';

describe('polls 配置', () => {
  it('缺 key 时兜底默认值', () => {
    const db = openDb(':memory:');
    expect(readPoll(db, 'tag')).toEqual({ intervalMs: 2000, batch: 16 });
    expect(readPoll(db, 'tagcheck')).toEqual({ intervalMs: 3000, batch: 200 });
    expect(readPoll(db, 'proposals')).toEqual({ intervalMs: 3000, batch: null });
  });

  it('writePoll 后读回新值', () => {
    const db = openDb(':memory:');
    writePoll(db, 'tag', { intervalMs: 5000, batch: 50 });
    expect(readPoll(db, 'tag')).toEqual({ intervalMs: 5000, batch: 50 });
  });

  it('库里的非法值兜底(手改库场景)', () => {
    const db = openDb(':memory:');
    setSetting(db, 'poll.tag.interval_ms', '99999');
    setSetting(db, 'poll.tag.batch', 'abc');
    expect(readPoll(db, 'tag')).toEqual({ intervalMs: 2000, batch: 16 });
  });

  it('listPolls 返回全部白名单任务', () => {
    const db = openDb(':memory:');
    expect(listPolls(db)).toEqual({
      tag: { intervalMs: 2000, batch: 16 },
      tagcheck: { intervalMs: 3000, batch: 200 },
      proposals: { intervalMs: 3000, batch: null },
    });
  });

  it('未知 taskType:readPoll 兜底不抛,writePoll 抛', () => {
    const db = openDb(':memory:');
    expect(readPoll(db, 'nope')).toEqual({ intervalMs: 2000, batch: 16 });
    expect(() => writePoll(db, 'nope', { intervalMs: 2000, batch: 16 })).toThrow();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd server && npx vitest run src/db/repo/state.test.ts`
Expected: FAIL — readPoll 未导出

- [ ] **Step 3: 实现**(追加到 state.ts)

```ts
// ── 批次任务轮询/批次配置(spec 2026-09-20 §3)──────────────
// 每个批次/AI 任务的轮询间隔与批次大小,按任务一对一存 settings。
// **settings 是缓存不是真相**:缺 key/非法值兜底默认表 —— 设置页还没用过、
// 或有人手改库写进垃圾值,任务都照常跑,不该挂在这。

export type PollConfig = { intervalMs: number; batch: number | null };

export const POLL_TASKS = ['tag', 'tagcheck', 'proposals'] as const;
export type PollTask = (typeof POLL_TASKS)[number];

export const POLL_INTERVALS = [1000, 2000, 3000, 5000, 10000];
export const POLL_BATCHES = [1, 2, 5, 10, 20, 30, 40, 50];

const DEFAULTS: Record<PollTask, PollConfig> = {
  tag: { intervalMs: 2000, batch: 16 },
  tagcheck: { intervalMs: 3000, batch: 200 },
  proposals: { intervalMs: 3000, batch: null }, // 一次 LLM 调用,无批次语义
};

const pollKey = (task: string, what: 'interval_ms' | 'batch') => `poll.${task}.${what}`;

const isPollTask = (t: string): t is PollTask => (POLL_TASKS as readonly string[]).includes(t);

export function readPoll(db: Database.Database, taskType: string): PollConfig {
  if (!isPollTask(taskType)) {
    console.warn(`[polls] 未知任务类型 ${taskType},返回 tag 默认`);
    return DEFAULTS.tag;
  }
  const def = DEFAULTS[taskType];
  const rawIv = Number(getSetting(db, pollKey(taskType, 'interval_ms')));
  const intervalMs = POLL_INTERVALS.includes(rawIv) ? rawIv : def.intervalMs;
  let batch = def.batch;
  if (def.batch !== null) {
    const raw = Number(getSetting(db, pollKey(taskType, 'batch')));
    if (Number.isInteger(raw) && raw >= 1 && raw <= 500) batch = raw; // 自定义档 1~500 都算合法
  }
  return { intervalMs, batch };
}

/**
 * 写单任务配置。**repo 层不校验**(校验在路由层 —— 两处对"什么是合法"必须给同一个答案,
 * 但只有路由对着用户,所以闸放那里)。batch 为 null/undefined 且任务有批次语义时,
 * 视为"恢复默认" —— 删 key,读时兜底。
 */
export function writePoll(
  db: Database.Database,
  taskType: string,
  cfg: { intervalMs: number; batch?: number | null },
): void {
  if (!isPollTask(taskType)) throw new Error(`未知任务类型 ${taskType}`);
  setSetting(db, pollKey(taskType, 'interval_ms'), String(cfg.intervalMs));
  if (DEFAULTS[taskType].batch === null) return; // proposals 无 batch 键
  if (cfg.batch === null || cfg.batch === undefined) {
    deleteSetting(db, pollKey(taskType, 'batch'));
    return;
  }
  setSetting(db, pollKey(taskType, 'batch'), String(cfg.batch));
}

export function listPolls(db: Database.Database): Record<string, PollConfig> {
  const out: Record<string, PollConfig> = {};
  for (const t of POLL_TASKS) out[t] = readPoll(db, t);
  return out;
}
```

注意:需要从同文件已有导出里用 `getSetting/setSetting`;`deleteSetting` 若不存在,补一行(与 setSetting 同款,`DELETE FROM settings WHERE key=?` —— 先查该文件确认,已有就直接用)。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd server && npx vitest run src/db/repo/state.test.ts`
Expected: PASS

---

### Task 2: polls 路由(GET/PUT + 校验)

**Files:**
- Create: `server/src/curator/settingsPolls.ts`
- Modify: `server/src/http/index.ts`(注册)
- Test: `server/src/curator/settingsPolls.test.ts`

**Interfaces:**
- Consumes: Task 1 全部导出。
- Produces:
  - `GET /api/settings/polls` → `{ polls: Record<string, PollConfig> }`
  - `PUT /api/settings/polls/:taskType` body `{ intervalMs: number; batch?: number | null }` → `{ok:true}`;非法 400 带中文 reason;未知 taskType 404
  - `registerPollsRoutes(app, { db })`

- [ ] **Step 1: 写失败测试**

```ts
// server/src/curator/settingsPolls.test.ts
import { describe, it, expect } from 'vitest';
import { openDb } from '../db/index.js';
import { Logger } from '../logger/index.js';
import { createServer } from '../http/index.js';
import type { BiliClient } from '../bilibili/client.js';

const stubClient = { withCredentials: () => ({ get: async () => null }) } as unknown as BiliClient;

function makeApp() {
  const db = openDb(':memory:');
  const log = new Logger(db, { silent: true });
  return { app: createServer({ db, log, client: stubClient }) };
}

const put = (app: Awaited<ReturnType<typeof makeApp>>['app'], task: string, payload: unknown) =>
  app.inject({ method: 'PUT', url: `/api/settings/polls/${task}`, payload });

describe('polls 路由', () => {
  it('GET 返回三个任务的配置(缺 key 全兜底)', async () => {
    const { app } = makeApp();
    const res = await app.inject({ url: '/api/settings/polls' });
    expect(res.statusCode).toBe(200);
    expect(res.json().polls).toEqual({
      tag: { intervalMs: 2000, batch: 16 },
      tagcheck: { intervalMs: 3000, batch: 200 },
      proposals: { intervalMs: 3000, batch: null },
    });
  });

  it('PUT 合法值(预设档)生效', async () => {
    const { app } = makeApp();
    const res = await put(app, 'tag', { intervalMs: 5000, batch: 40 });
    expect(res.statusCode).toBe(200);
    const get = await app.inject({ url: '/api/settings/polls' });
    expect(get.json().polls.tag).toEqual({ intervalMs: 5000, batch: 40 });
  });

  it('PUT 自定义 batch(120,在 1~500 内)生效', async () => {
    const { app } = makeApp();
    const res = await put(app, 'tagcheck', { intervalMs: 1000, batch: 120 });
    expect(res.statusCode).toBe(200);
    const get = await app.inject({ url: '/api/settings/polls' });
    expect(get.json().polls.tagcheck.batch).toBe(120);
  });

  it('PUT 非法 intervalMs → 400', async () => {
    const { app } = makeApp();
    const res = await put(app, 'tag', { intervalMs: 7777, batch: 16 });
    expect(res.statusCode).toBe(400);
    expect(res.json().reason).toContain('轮询间隔');
  });

  it('PUT 非法 batch(0 / 999 / 小数)→ 400', async () => {
    const { app } = makeApp();
    for (const batch of [0, 999, 2.5]) {
      const res = await put(app, 'tag', { intervalMs: 2000, batch });
      expect(res.statusCode).toBe(400);
      expect(res.json().reason).toContain('批次');
    }
  });

  it('PUT proposals 忽略 batch 字段', async () => {
    const { app } = makeApp();
    const res = await put(app, 'proposals', { intervalMs: 10000, batch: 40 });
    expect(res.statusCode).toBe(200);
    const get = await app.inject({ url: '/api/settings/polls' });
    expect(get.json().polls.proposals).toEqual({ intervalMs: 10000, batch: null });
  });

  it('未知 taskType → 404', async () => {
    const { app } = makeApp();
    const res = await put(app, 'nope', { intervalMs: 2000, batch: 16 });
    expect(res.statusCode).toBe(404);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd server && npx vitest run src/curator/settingsPolls.test.ts`
Expected: FAIL — 404(路由不存在)

- [ ] **Step 3: 实现路由**

```ts
// server/src/curator/settingsPolls.ts
import type { FastifyInstance } from 'fastify';
import type Database from 'better-sqlite3';
import {
  listPolls, writePoll, POLL_TASKS, POLL_INTERVALS, POLL_BATCHES,
} from '../db/repo/state.js';

export interface PollsDeps { db: Database.Database }

/**
 * 任务三件套设置的两条路由(spec 2026-09-20 §3)。
 * 校验在这一层:repo 的 writePoll 不校验 —— 只有这里对着用户。
 */
export function registerPollsRoutes(app: FastifyInstance, deps: PollsDeps): void {
  const { db } = deps;

  app.get('/api/settings/polls', async () => ({ polls: listPolls(db) }));

  app.put('/api/settings/polls/:taskType', async (req, reply) => {
    const taskType = (req.params as { taskType: string }).taskType;
    if (!(POLL_TASKS as readonly string[]).includes(taskType)) {
      return reply.code(404).send({ ok: false, reason: `未知任务类型 ${taskType}` });
    }
    const body = (req.body ?? {}) as { intervalMs?: unknown; batch?: unknown };
    const iv = Number(body.intervalMs);
    if (!POLL_INTERVALS.includes(iv)) {
      return reply.code(400).send({ ok: false, reason: '轮询间隔必须是 1/2/3/5/10 秒之一' });
    }
    // proposals 无批次概念 —— batch 字段来了也忽略(spec §0 修正 1)
    if (taskType === 'proposals') {
      writePoll(db, taskType, { intervalMs: iv });
      return { ok: true };
    }
    const b = Number(body.batch);
    const preset = POLL_BATCHES.includes(b);
    const custom = Number.isInteger(b) && b >= 1 && b <= 500;
    if (!preset && !custom) {
      return reply.code(400).send({ ok: false, reason: '批次大小必须是预设档位或 1~500 的整数' });
    }
    writePoll(db, taskType, { intervalMs: iv, batch: b });
    return { ok: true };
  });
}
```

- [ ] **Step 4: 注册路由**

`server/src/http/index.ts`:import + `registerPollsRoutes(app, { db });`(挨着 registerRuleRoutes 那排)。

- [ ] **Step 5: 全量回归**

Run: `cd server && npx vitest run && npx tsc --noEmit`
Expected: 全绿。

---

### Task 3: 前端类型 + api client

**Files:**
- Modify: `web/src/types.ts`(末尾追加)
- Modify: `web/src/api.ts`(rulesApi 之后追加)

- [ ] **Step 1: types.ts 末尾追加**

```ts
// ── 批次任务三件套设置(spec 2026-09-20 §3)──────────────
export type PollConfig = { intervalMs: number; batch: number | null };
export type PollsMap = Record<string, PollConfig>;
```

- [ ] **Step 2: api.ts 追加**(types import 行把 PollConfig/PollsMap 加进去)

```ts
// ── 批次任务三件套设置 ──────────────────────────────────

export const settingsApi = {
  getPolls: () => api<{ polls: PollsMap }>('/api/settings/polls').then((r) => r.polls),
  setPoll: (taskType: string, cfg: { intervalMs: number; batch?: number | null }) =>
    json<{ ok: true }>('PUT', `/api/settings/polls/${taskType}`, cfg),
};
```

- [ ] **Step 3: 门禁**

Run: `cd web && npx tsc --noEmit`
Expected: PASS

---

### Task 4: TaskLogDrawer 泛型抽屉 + useTaskProgress hook

**Files:**
- Create: `web/src/components/TaskLogDrawer.tsx`
- Create: `web/src/hooks/useTaskProgress.ts`

**Interfaces:**
- Produces(B/C 依赖,签名逐字):

```tsx
// TaskLogDrawer —— 泛型壳
export default function TaskLogDrawer<T>({
  open, onClose, onClear, waiting, title,
  lines, renderLine, serialize, downloadName, warnCount,
}: {
  open: boolean;
  onClose: () => void;
  onClear: () => void;
  /** 任务在跑但日志还没内容 → 空态说"正在等模型" */
  waiting?: boolean;
  title: string;                    // 如「标注日志」
  lines: T[];                       // 全量缓冲区
  renderLine: (line: T, index: number) => ReactNode;
  /** 下载内容;缺省 JSON.stringify(lines, null, 2) */
  serialize?: (lines: T[]) => string;
  downloadName?: string;            // 缺省 `${title}-${日期}.txt`
  /** warn 行数;泛型壳不知道哪行是 warn,由调用方数好传入 */
  warnCount?: number;
});
```

```ts
// useTaskProgress —— 薄轮询 hook
export function useTaskProgress<T>(opts: {
  taskType: string;          // 查 poll.<taskType>.interval_ms
  fetcher: () => Promise<T>;
  enabled: boolean;          // true 才轮询
}): T | null;
```

- [ ] **Step 1: 实现TaskLogDrawer.tsx**

以 `web/src/components/TagLogDrawer.tsx` 为底本改造(先 Read 它),保留:贴底跟随(含 40px 离底停跟随)、500 行渲染封顶(缓冲区不截)、空态分支、Drawer 骨架与样式。替换:

1. props 换成上面的泛型签名;`TagLogLine` 相关全部去掉
2. 标题区:`{title}` 代替「标注日志」;行数后显示 `warnCount`(传了且 >0 才显示「 · N 条提醒」)
3. 图标区**三个**按钮:贴底(ArrowDown,照抄)、清空(Eraser,照抄)、**下载(Download)**:

```tsx
<Button
  type="text" size="small" aria-label="下载日志"
  icon={<Download size={14} />}
  onClick={() => {
    const text = serialize ? serialize(lines) : JSON.stringify(lines, null, 2);
    const url = URL.createObjectURL(new Blob([text], { type: 'text/plain;charset=utf-8' }));
    const a = document.createElement('a');
    a.href = url;
    a.download = downloadName ?? `${title}-${new Date().toISOString().slice(0, 10)}.txt`;
    a.click();
    URL.revokeObjectURL(url);
  }}
/>
```

4. 渲染区:`visible.map((l, i) => <Fragment key={i}>{renderLine(l, i)}</Fragment>)`;空态文案通用化:「任务在跑 —— 正在等模型返回第一批结果……」/「还没跑过 —— 点上面的启动按钮,这里会出现每一步」
5. `changes` 区块**不搬**(那是标签页特有的,留在 TagLogDrawer 自己的抽屉里)

- [ ] **Step 2: 实现useTaskProgress.ts**

```ts
import { useEffect, useRef, useState } from 'react';
import { settingsApi } from '../api';

/**
 * 任务轮询 hook(spec 2026-09-20 §4.1)。**刻意薄**:只把「间隔可配」抽出来,
 * 业务状态(running/done/total)由调用方从 fetcher 结果里自己读 ——
 * 和标注/质检现有轮询一样是哑轮询,不做任务状态机。
 */
export function useTaskProgress<T>(opts: {
  taskType: string;
  fetcher: () => Promise<T>;
  enabled: boolean;
}): T | null {
  const { taskType, fetcher, enabled } = opts;
  const [latest, setLatest] = useState<T | null>(null);
  const [intervalMs, setIntervalMs] = useState(3000); // 配置拉不到的兜底(spec §5)
  const fetcherRef = useRef(fetcher);
  fetcherRef.current = fetcher; // 每渲染更新,避免 effect 依赖 fetcher 而反复重建 interval

  useEffect(() => {
    let alive = true;
    settingsApi.getPolls()
      .then((polls) => {
        if (alive && polls[taskType]) setIntervalMs(polls[taskType].intervalMs);
      })
      .catch(() => {}); // 配置失败用默认,轮询照常
    return () => { alive = false; };
  }, [taskType]);

  useEffect(() => {
    if (!enabled) return;
    // enabled 变 true 立即拉一次 —— 不然启动后要干等一个完整间隔才见第一帧
    const tick = () => { void fetcherRef.current().then(setLatest).catch(() => {}); };
    tick();
    const t = setInterval(tick, intervalMs);
    return () => clearInterval(t);
  }, [enabled, intervalMs]);

  return latest;
}
```

- [ ] **Step 3: 门禁**

Run: `cd web && npx tsc --noEmit && npx max build 2>&1 | grep -i "compiled successfully"`
Expected: 两者都过。

---

### Task 5: TaskSettings 合一表(ModelManager 重命名 + 加两列)

**Files:**
- Create: `web/src/components/TaskSettings.tsx`(= ModelManager.tsx 改造版)
- Delete: `web/src/components/ModelManager.tsx`(`rm`,不用 git rm)
- Modify: `web/src/pages/auth.tsx`(import 与用法)

**Interfaces:**
- Consumes: Task 3 `settingsApi`、现有 `llmApi.setAssignments`。
- Produces: 默认导出 `TaskSettings` 组件;每个 purpose 卡片 = 模型下拉(已有)+ 轮询间隔下拉(新)+ 批次大小下拉(新;`chat`/`rules`/`classify` 不显示批次)。

- [ ] **Step 1: 读 ModelManager.tsx 全文**(约 700+ 行),弄清:
  - purpose 列表怎么来(types.ts `LlmPurpose` 五项 + label)
  - 模型下拉与 `change(purpose, v)` → `llmApi.setAssignments` 的链路
  - 卡片布局结构

- [ ] **Step 2: 写 TaskSettings.tsx**(保守改造 —— 布局不动,只加东西):

1. 组件改名 `TaskSettings`,页面标题文案「模型管理」→「任务设置」,副标题注明「每个任务:AI 模型 / 轮询间隔 / 批次大小」
2. 组件挂载时 `settingsApi.getPolls()` 存 state(拉失败红条,沿用文件既有错误显示惯例)
3. purpose→taskType 映射:`tag`/`tagcheck`/`proposals` 显示三件套;`chat`/`rules`/`classify` 只有模型列
4. 轮询间隔 Select:选项 `1s/2s/3s/5s/10s`(值 1000/2000/3000/5000/10000),当前值从 polls state 读;onChange → `settingsApi.setPoll(taskType, { intervalMs: v, batch: 当前batch })` → 成功后更新本地 polls state
5. 批次 Select(poll 任务才有):预设 `1/2/5/10/20/30/40/50` + 「自定义…」选项;选自定义时用 antd `Modal.confirm` 内嵌 Input(数字 1~500)取值(参照 TagPanel `confirmClearTags` 的非受控 Input 写法);onChange → `setPoll(taskType, { intervalMs: 当前, batch: v })`;`proposals` 卡不渲染此列,显示「—」
6. 每张卡片底部小字:「轮询间隔对已打开页面在下次进入该页时生效;批次大小下次启动任务时生效」(如实 —— Plan A 阶段旧任务还是硬编码轮询)

- [ ] **Step 3: 删旧文件 + 改引用**

`rm web/src/components/ModelManager.tsx`;`auth.tsx` 里 `import ModelManager from '../components/ModelManager'` → `import TaskSettings from '../components/TaskSettings'`,JSX `<ModelManager />` → `<TaskSettings />`。grep 确认无其他引用。

- [ ] **Step 4: 门禁**

Run: `cd web && npx tsc --noEmit && npx max build 2>&1 | grep -i "compiled successfully"`
Expected: 全过。

---

## 手动验收清单(Plan A 完成后用户过一遍)

1. 启动前后端,进「授权」页 → 看到「任务设置」表;标注/质检/方案生成三行各有模型下拉,标注和质检多轮询/批次两列,方案生成批次列显示「—」
2. 改标注轮询 5s → 刷新页面仍在(存进库了)
3. 改批次为「自定义 120」→ 弹输入框 → 保存 → 刷新仍在
4. `curl http://localhost:<port>/api/settings/polls` 返回三任务配置,值与你设的一致
5. 标注/质检/方案生成功能照旧能用(本 plan 不动它们)

## Self-Review 记录

- Spec 覆盖:§3 后端=Task 1+2;§3 前端类型=Task 3;§4.1/4.2=Task 4;§4.3=Task 5。B/C 依赖产物齐(POLL_* 常量、settingsApi、useTaskProgress、TaskLogDrawer)。
- 类型一致:`PollConfig.batch: number|null` 贯穿 repo→路由→api→设置页;proposals 的 null 特判在 readPoll/writePoll/路由三处口径一致。
- 风格:所有代码块为逐字可用版本(无占位笔误);Task 4/5 的「照抄 TagLogDrawer」处给了行号与语义,执行者先 Read 底本再改。
