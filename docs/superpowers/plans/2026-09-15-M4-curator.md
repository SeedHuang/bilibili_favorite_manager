# M4 整理(AI 管家)— 实施计划框架

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户能用 AI 整理 bilibili 收藏(聊天 → 体系 → 抽样归类 → 手动编辑 → 审计)。

**Architecture:**
- 后端用 **Vercel AI SDK**(`ai@7.x` + `@ai-sdk/deepseek` + `@ai-sdk/ollama`),模型注册表 + 上下文自适应
- 两遍分类:**§9.0 keyword 初分**(本地规则,0 token)+ **§9.1 AI 抽检校准**(100 条,2 阶段)
- AI 助手 = **常驻悬浮图标**(右下角),默认动作「整理文件夹」,调出聊天窗
- /curator 页面 = **手动编辑工作台**(可手动移/合并/改),AI 流程在聊天窗里跑
- 会话存 SQLite,**历史可续** + **上下文压缩**(最近 N 轮 + 旧轮滚动摘要 + taxonomy_draft 快照)
- 整理审计报告 → `audit_logs` 表(reorganize kind)

**Tech Stack:** Vercel AI SDK 7.x、`@ai-sdk/deepseek` 3.x、`better-sqliteite` 12.11.1(已用)、SSE(已用)、antd 5 + lucide

**Spec:** `docs/superpowers/specs/2026-09-14-bilibili-favorite-manager-design.md` (§3 LLM 接入层、§5 表、§9.0 整理流程、§9.1/9.1.1/9.3 分两遍、§10.1.1 删除保险、§11.4 页面 + 11.9 AI 助手)

**M5 写回不在这**(写回 B 站是单独的计划)。

---

## Global Constraints

从 spec 摘出对 M4 全程生效的硬约束:

- Node 22.12、TypeScript ^5.9.3、better-sqliteite ^12.11.1、ESM(`import type` for types)
- `strict` + `noUncheckedIndexedAccess`
- 依赖方向:`http → curator/plan/execute → plan/db/llm/logger → bilibili`
- **§10.1.1 删除保险**:AI 永不产生 remove_item,只能 userId 手动
- **§9.1.1 输出校验**:Pass 1 输出后硬校验 reuseFolderId 不存在/重复/未复用
- **§10.6.3 写回快照**(虽然 M5 才用,M4 写报告/审计时要注意)
- 中文注释;commit trailer `Co-Authored-By: Claude Code <noreply@anthropic.com>`
- 全量测试通过(typecheck + npm test -w server + cd web && npm run typecheck)

---

## File Structure(M4 新增/改动)

```
server/src/
├─ llm/
│  ├─ provider.ts        # NEW: Vercel AI SDK 包装(createDeepSeek/createOllama + generateText/streamText)
│  ├─ registry.ts        # NEW: 模型注册表(15 个模型 + contextWindow + maxOutput + verified)
│  ├─ context.ts         # NEW: 上下文预算(按 contextWindow 算 batchSize、自动滚动压缩)
│  ├─ chat.ts            # NEW: 聊天封装(messages + system + 流式)
│  ├─ classifier.ts      # NEW: §9.0 两遍分类(纯函数,无 IO)
│  └─ classifier.test.ts # NEW: 分类器单元测试
├─ curator/
│  ├─ keyword.ts         # NEW: §9.0 keyword 规则(纯函数,可测)
│  ├─ keyword.test.ts    # keyword 单元测试
│  └─ routes.ts          # NEW: /api/curator/* 路由(开始整理、聊天、回滚、审计)
├─ sync/                 # 不动,只读
├─ db/                   # 不动结构,加 3 张 M4 表的 migration
├─ http/                 # 把 curator 路由接进来(在 createServer 里)

web/src/
├─ api.ts                # MODIFY: 加 curator 接口封装
├─ types.ts              # MODIFY: 加 ChatMessage / TaxonomyDraft / AuditReport 类型
├─ components/
│  ├─ AIAssistantIcon.tsx   # NEW: 悬浮图标(常驻,右下角)
│  ├─ ChatDrawer.tsx        # NEW: 聊天抽屉(对话 + 流式 + 历史列表)
│  ├─ TaxonomyDraftEditor.tsx # NEW: 体系草稿编辑(改名/合并/拆分)
│  └─ PlanDiffView.tsx     # NEW: 整理前后对比 + 撤回
├─ pages/
│  ├─ curator.tsx        # NEW: 手动编辑工作台(默认页)
│  └─ settings.tsx       # MODIFY: 加模型管理(选服务商 → 选模型 → 测试连接)
└─ layouts/index.tsx     # MODIFY: 渲染 AIAssistantIcon(全局常驻)
```

不要碰:`server/src/bilibili/`、`server/src/sync/`(M4 只读)、`server/src/probes/`、`server/src/dev.ts`

---

## 任务编号(占位,下面会补完整步骤)

> **下面是我把每个任务写完前的框架版本**。接下来会逐任务填完整步骤。这一步先确定
> 顺序和接口,后面再扩。如果你觉得任务切分有问题,现在提,我重排。

### Task 1: 数据库 migration — sessions / session_messages / taxonomy_draft / audit_logs

**Files:**
- Modify: `server/src/db/schema.ts`(已追加 4 张 M4 表)
- Create: `server/src/db/migrations.test.ts`(验证迁移存在 + 幂等)

**Interfaces:**
- Consumes: `applySchema(db)` from M3
- Produces: 4 张新表已存在 + 幂等可重复调用

schema 已加(051999c commit),剩测试与验证。

- [ ] **Step 1:写测试验证新表存在**
- [ ] **Step 2:跑测试确认通过(不写失败测试,先验证存在)**
- [ ] **Step 3:`npm test -w server -- src/db/migrations.test.ts` 全绿**
- [ ] **Step 4:`npm test -w server` 全量绿(180 + 4 新 = 184)**
- [ ] **Step 5:commit**

`server/src/db/migrations.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { openDb, applySchema } from './index.js';

describe('M4 schema migration', () => {
  it('sessions / session_messages / taxonomy_draft 表存在', () => {
    const db = openDb(':memory:');
    applySchema(db);
    const tables = (db.prepare(
      `SELECT name FROM sqlite_master WHERE type='table' AND name IN (?, ?, ?, ?)`,
    ).all('sessions', 'session_messages', 'taxonomy_draft', 'audit_logs') as { name: string }[])
      .map(r => r.name);
    expect(tables).toEqual(expect.arrayContaining(['sessions', 'session_messages', 'taxonomy_draft', 'audit_logs']));
  });

  it('applySchema 幂等', () => {
    const db = openDb(':memory:');
    expect(() => applySchema(db)).not.toThrow();
    expect(() => applySchema(db)).not.toThrow();
  });

  it('sessions 表支持 status 列(active | archived)', () => {
    const db = openDb(':memory:');
    applySchema(db);
    db.prepare(`INSERT INTO sessions (title, status) VALUES (?, ?)`).run('test', 'active');
    const r = db.prepare(`SELECT status FROM sessions WHERE title = ?`).get('test') as { status: string };
    expect(r.status).toBe('active');
  });

  it('session_messages.session_id 外键指向 sessions', () => {
    const db = openDb(':memory:');
    applySchema(db);
    db.prepare(`INSERT INTO sessions (title, status) VALUES (?, ?)`).run('t', 'active');
    db.prepare(`INSERT INTO session_messages (session_id, role, content) VALUES (?, ?, ?)`).run(1, 'user', 'hi');
    expect(db.prepare(`SELECT COUNT(*) AS n FROM session_messages`).get() as { n: number }).toEqual({ n: 1 });
  });

  it('taxonomy_draft 是 sessions 的一对一', () => {
    const db = openDb(':memory:');
    applySchema(db);
    db.prepare(`INSERT INTO sessions (id, title, status) VALUES (?, ?, ?)`).run(42, 't', 'active');
    db.prepare(`INSERT INTO taxonomy_draft (session_id, folders_json) VALUES (?, ?)`).run(42, '[]');
    const r = db.prepare(`SELECT folders_json FROM taxonomy_draft WHERE session_id = ?`).get(42) as { folders_json: string };
    expect(r.folders_json).toBe('[]');
  });
});
```

Step 1-2 写完直接跑(确认 schema 加对了)。

Step 4 跑 `npm test -w server` 全量(应绿,180 + 4 新 = 184)。

Step 5 commit:

```bash
git add server/src/db/migrations.test.ts
git commit -m "test(db): M4 schema migration 存在 + 幂等测试"
```

---

### Task 2: LLM provider + registry + context 预算

**Files:**
- Create: `server/src/llm/registry.ts`
- Create: `server/src/llm/context.ts`
- Create: `server/src/llm/provider.ts`
- Create: `server/src/llm/registry.test.ts`
- Create: `server/src/llm/context.test.ts`
- Create: `server/src/llm/provider.test.ts`
- Modify: `package.json` 加 `ai@^7.x` + `@ai-sdk/deepseek@^3.x` + `@ai-sdk/ollama`

**Interfaces:**
- Consumes: Vercel AI SDK + model config(从 settings 表读)
- Produces:
  ```ts
  interface ModelMeta {
    provider: 'ollama' | 'ark' | 'deepseek' | 'minimax' | 'anthropic-compatible' | 'custom';
    model: string;
    contextWindow: number;
    maxOutput: number;
    verified: boolean;
    note?: string;
  }
  interface ModelConfig {
    id: string;
    baseUrl: string;
    apiKey: string;
    model: string;
  }
  function getModelMeta(provider: string, model: string): ModelMeta
  function batchSize(ctx: ModelMeta): number
  function trimToContext(messages: Message[], ctx: ModelMeta): Message[]
  async function complete(opts: { config: ModelConfig; prompt: string }): Promise<string>
  async function stream(opts: { config: ModelConfig; prompt: string; onChunk: (s: string) => void }): Promise<void>
  ```

**§3 模型注册表(实测过的数据)**:

```ts
// server/src/llm/registry.ts
const MODELS: Record<string, ModelMeta> = {
  // 火山方舟 Coding Plan(实测确认)
  'ark-code-latest':          { provider: 'ark', model: 'ark-code-latest', contextWindow: 256_000,  maxOutput: 32_000,  verified: true, note: '控制台选定的实际模型' },
  'claude-sonnet-4.5':         { provider: 'ark', model: 'claude-sonnet-4.5', contextWindow: 200_000,  maxOutput: 64_000,  verified: true, note: '1M 需 Beta' },
  'claude-opus-4.1':           { provider: 'ark', model: 'claude-opus-4.1', contextWindow: 200_000,  maxOutput: 64_000,  verified: true },
  // 火山方舟通用(实测确认)
  'doubao-seed-evolving':     { provider: 'ark', model: 'doubao-seed-evolving', contextWindow: 1_024_000, maxOutput: 256_000, verified: true, note: '输出与输入同量级' },
  'doubao-seed-2.1-turbo':    { provider: 'ark', model: 'doubao-seed-2.1-turbo', contextWindow: 256_000, maxOutput: 256_000, verified: true },
  'doubao-seed-2.0-lite':     { provider: 'ark', model: 'doubao-seed-2.0-lite', contextWindow: 256_000, maxOutput: 128_000, verified: true },
  'minimax-m3':               { provider: 'ark', model: 'minimax-m3', contextWindow: 1_024_000, maxOutput: 128_000, verified: true, note: '方舟端限 128K(M3 原生更大)' },
  'glm-5.3':                  { provider: 'ark', model: 'glm-5.3', contextWindow: 1_024_000, maxOutput: 128_000, verified: true },
  'glm-5.3-flash':            { provider: 'ark', model: 'glm-5.3-flash', contextWindow: 1_024_000, maxOutput: 128_000, verified: true },
  'deepseekk-v4-flash':       { provider: 'ark', model: 'deepseekk-v4-flash', contextWindow: 1_024_000, maxOutput: 384_000, verified: true },
  'deepseek-v4-pro':          { provider: 'ark', model: 'deepseek-v4-pro', contextWindow: 1_024_000, maxOutput: 384_000, verified: true },
  'kimi-k2.7-code':           { provider: 'ark', model: 'kimi-k2.7-code', contextWindow: 256_000, maxOutput: 32_000, verified: true, note: 'K2.7 ≠ K3' },
  'kimi-k3':                  { provider: 'ark', model: 'kimi-k3', contextWindow: 1_024_000, maxOutput: 128_000, verified: true },
  // DeepSeek 普通 API(已确认)
  'deepseek-chat':            { provider: 'deepseek', model: 'deepseek-chat', contextWindow: 128_000, maxOutput: 8_192, verified: true },
  'deepseek-reasoner':        { provider: 'deepseek', model: 'deepseek-reasoner', contextWindow: 128_000, maxOutput: 8_192, verified: true },
  // MiniMax
  'MiniMax-M2.7':            { provider: 'minimax', model: 'MiniMax-M2.7', contextWindow: 204_800, maxOutput: 8_192, verified: true, note: '估' },
};
export function getModelMeta(provider: string, model: string): ModelMeta { ... }
export function batchSize(ctx: ModelMeta): number { ... }
```

**测试驱动步骤**:

- [ ] Step 1:写 `registry.test.ts`:查已知模型(matching) + 查未知模型(throw 或 fallback)
- [ ] Step 2:跑失败
- [ ] Step 3:实现 `registry.ts`
- [ ] Step 4:跑绿
- [ ] Step 5:写 `context.test.ts`:batchSize 本地 14b(32K)≥ 100 条批,1M 上下文 = 全塞
- [ ] Step 6-7:实现 + 跑绿
- [ ] Step 8:写 `provider.test.ts`:mock fetch,验证 generateText 用对模型和 headers
- [ ] Step 9-10:实现 + 跑绿(注意:test 用 `vi.mock('ai')` 拦截,不调真实 SDK)
- [ ] Step 11:`npm run typecheck` 绿
- [ ] Step 12:commit

> **⚠️ 测试与 API 兼容**:Vercel AI SDK 7.x 是 CommonJS-friendly 重大版。装包前确认 package.json 已有 `"ai": "^7.0.0"` 与 `"@ai-sdk/deepseek": "^3.0.0"`(用 `npm install` 而不是手改)。

---

### Task 3: chatbot(§9.0 聊天流)

**Files:**
- Create: `server/src/curator/chat.ts`
- Create: `server/src/curator/chat.test.ts`
- Create: `server/src/db/repo/sessions.ts`(session / message / draft CRUD)

**Interfaces:**
- Consumes: Task 2 的 provider(provider.ts / context.ts)
- Produces:
  ```ts
  function newSession(db, title: string): number  // returns sessionId
  function appendMessage(db, sessionId: number, role, content): number
  function getMessages(db, sessionId: number, limit?: number): SessionMessage[]
  function getLatestDraft(db, sessionId: number): Draft | null
  function upsertDraft(db, sessionId, foldersJson, constraintsJson?): void
  // 上下文压缩:最近 N 轮 + taxonomy_draft 快照 + 旧轮摘要
  function buildContext(db, sessionId: number, ctx: ModelMeta): { messages: Message[]; draft: Draft | null; summary: string }
  // 流式聊天 + 自动落库
  async function chatStream(opts: {
    db: Database.Database;
    sessionId: number;
    config: ModelConfig;
    userMessage: string;
    draftSnapshot: Draft | null;
  }): AsyncIterable<string>  // SSE-friendly
  ```

**关键**:
- 流式产出的 chunk 立即 `appendMessage(role='assistant', content=cumulative)`
- 上下文预算:`recent 8 轮原文 + draft 快照 + 旧轮摘要(已压缩)+ system prompt`
- 草稿变化(`taxonomy_draft`)不压缩,只压缩"聊天"

**测试驱动步骤**:

- [ ] Step 1:写 `sessions.ts` CRUD 测试:insert/get/update/draft
- [ ] Step 2:跑失败 → 实现 → 跑绿
- [ ] Step 3:写 `chat.ts` 测试(monkey-patch provider stream,验证 SSE 风格产出 + 自动存库)
- [ ] Step 4:跑失败 → 实现 → 跑绿
- [ ] Step 5:`npm test -w server` 全绿
- [ ] Step 6:commit

---

### Task 4: keyword 规则(§9.0 step 1)

**Files:**
- Create: `server/src/curator/keyword.ts`
- Create: `server/src/curator/keyword.test.ts`

**Interfaces:**
- Consumes: 纯函数,无 IO
- Produces:
  ```ts
  interface KeywordHit {
    candidate: string;     // 候选夹子名
    confidence: number;    // 0-1
    matchedField: 'title' | 'intro' | 'upper';
    matchedToken: string;
  }
  function keywordClassify(item: { title; intro; upperName; duration? }, rules: Map<string, string[]>): KeywordHit[]
  ```

**测试驱动步骤**(用真实数据风格 fixture):
- [ ] Step 1:写测试:Python 关键词 → "AI/编程"
- [ ] Step 2:跑失败 → 实现 → 跑绿
- [ ] Step 3:commit

---

### Task 5: 两遍分类器(§9.0 完整)

**Files:**
- Create: `server/src/curator/classifier.ts`
- Create: `server/src/curator/classifier.test.ts`

**Interfaces:**
- Consumes: KBTask 2 的 context / provider,Task 4 的 keyword,Task 3 的 chat 流
- Produces:
  ```ts
  interface ClassifyResult {
    taxonomy: TaxonomyProposal;
    validated: ValidationReport;
    assignments: Assignment[];  // 每个 item 一个 { itemId, folderTempId, confidence, reason }
  }
  interface ValidationReport {
    invalidReuseIds: number[];        // reuseFolderId 引用不存在的 id
    duplicateReuseIds: [number, number][];
    unmatchedExistingFolders: number[]; // 现有夹子但体系没复用
    nameConflicts: string[];
  }
  async function classifyAll(opts: {
    db: Database.Database;
    sessionId: number;
    config: ModelConfig;
    ctx: ModelMeta;
    existingFolders: FolderLite[]; // id + name
    items: ItemRow[];                // 待归类
    userConstraint?: string;         // 「控制在 15 个以内」之类
  }): Promise<ClassifyResult>
  ```

**关键流程**:
1. `keywordClassify(items)` → 0 token,得"大致分法"
2. 抽样 100 条 balance → 喂模型 Pass 1,出 TaxonomyProposal
3. **§9.1.1 输出校验**:reuseFolderId 不存在/重复 → 报错;未复用现有夹子 → UI 警告
4. 通过后,按 taxonomy 分批归类,§10.6.2 batchSize 动态,按 14b 32K 约 120 条/批
5. 主动学习:高把握的作为种子,边界条目再问模型

**测试驱动步骤**:
- [ ] Step 1:写测试 Pass 1 校验(reuseFolderId 不存在 → throw;重复 → throw)
- [ ] Step 2-3:实现 + 跑绿
- [ ] Step 4:写测试 Pass 2 抽样(12000 items → batchSize 适配 32K)
- [ ] Step 5:跑失败 → 实现 → 跑绿
- [ ] Step 6:端到端:keyword → Pass 1 → 校验 → Pass 2(mock LLM,验证 assignments 全覆盖)
- [ ] Step 7:`npm test -w server` 全绿
- [ ] Step 8:commit

---

### Task 6: HTTP 路由(/api/curator)

**Files:**
- Create: `server/src/curator/routes.ts`
- Create: `server/src/curator/routes.test.ts`
- Modify: `server/src/http/index.ts`(把 curator 路由接进 createServer)

**Endpoints**(spec §9.0 流程):
```
POST   /api/curator/sessions                       开新会话
GET    /api/curator/sessions                       会话列表
GET    /api/curator/sessions/:id                   会话详情(含消息 + 草稿)
POST   /api/curator/sessions/:id/messages          发消息(SSE 流式)
POST   /api/curator/sessions/:id/run-pass-1        启动整理 Pass 1
POST   /api/curator/sessions/:id/run-pass-2        启动 Pass 2
GET    /api/curator/sessions/:id/draft              取草稿
PUT    /api/curator/sessions/:id/draft              保存草稿
DELETE /api/curator/sessions/:id                   归档
```

**所有路由注册到 `registerCuratorRoutes(app, deps)`**(沿用 M3 的 registerXxxRoutes 模式)。

**Tests**:每条路由用 fastify.inject() 跑,LLM provider mock 掉

- [ ] Step 1:写 routes.test.ts:每条路由 200/400 + 流式响应
- [ ] Step 2:跑失败 → 实现 → 跑绿
- [ ] Step 3:接进 index.ts
- [ ] Step 4:`npm test -w server` 全绿
- [ ] Step 5:commit

---

### Task 7: 前端 — types + API 封装

**Files:**
- Modify: `web/src/types.ts`
- Modify: `web/src/api.ts`

```ts
// web/src/types.ts 加
export interface ChatMessage {
  id: number;
  role: 'user' | 'assistant' | 'system';
  content: string;
  ts: number;
}
export interface SessionSummary {
  id: number;
  title: string;
  preview: string;
  status: 'active' | 'archived';
  updatedAt: number;
}
export interface SessionDetail extends SessionSummary {
  messages: ChatMessage[];
  draft: { folders: FolderSpec[]; constraints?: string; summary?: string } | null;
}
export interface FolderSpec {
  tempId: string;
  name: string;
  description: string;
  rule: string;
  estCount: number;
  reuseFolderId?: number;
}
export interface AuditReport {
  id: number;
  kind: 'reorganize' | 'sync';
  title: string;
  summary: string;
  createdAt: number;
}

// web/src/api.ts 加 curator 方法
async function listSessions(): Promise<SessionSummary[]>
async function getSession(id: number): Promise<SessionDetail>
async function newSession(title: string): Promise<number>
async function sendMessage(sessionId: number, content: string): Promise<ChatMessage>  // 或 SSE 流,看下面
async function runPass1(sessionId: number, constraint?: string): Promise<{ taxonomy: TaxonomyProposal; validation: ValidationReport }>
async function runPass2(sessionId: number): Promise<Assignment[]>
async function getDraft(sessionId: number)
async function saveDraft(sessionId: number, folders: FolderSpec[], constraints?: string)
async function deleteSession(id: number)
```

- [ ] Step 1:写 types 增补,确保 web typecheck 通过
- [ ] Step 2:写 api.ts 增补,用 jest.mock 或 msw 拦截
- [ ] Step 3:`cd web && npm run typecheck` 通过
- [ ] Step 4:commit

---

### Task 8: 前端 — AI 助手悬浮图标

**Files:**
- Create: `web/src/components/AIAssistantIcon.tsx`
- Modify: `web/src/layouts/index.tsx`(在 ConfigProvider 后渲染 `<AIAssistantIcon />`)

```tsx
// AIAssistantIcon.tsx — 全局常驻,右下角,圆形青色发光,点击调 ChatDrawer
// 用 morphicons 状态形变:default / active / working
// 角色图先用 placeholder(commit 时不写死,从社区素材选后注入)
```

- [ ] Step 1:写组件(像素风图标占位 + morphicons 状态)
- [ ] Step 2:`web build` 通过
- [ ] Step 3:接进 layouts
- [ ] Step 4:commit

---

### Task 9: 前端 — 聊天抽屉

**Files:**
- Create: `web/src/components/ChatDrawer.tsx`

```tsx
// 右侧抽屉,400px,深底 + 青色描边
// 顶部:会话标题 + 切换/新建按钮
// 中部:流式消息气泡(用户右,AI 左,mono 字体显示)
// 底部:输入框 + 发送按钮
// 流式:fetch('/api/curator/sessions/:id/messages', { method: POST, body: ... }) → ReadableStream
// 历史会话侧栏(可折叠):只显示 summary 一句话
```

- [ ] Step 1:写组件
- [ ] Step 2:`web build` 通过(TS 编译)
- [ ] Step 3:commit

---

### Task 10: 前端 — /curator 手动编辑工作台

**Files:**
- Create: `web/src/pages/curator.tsx`
- Create: `web/src/components/TaxonomyDraftEditor.tsx`
- Create: `web/src/components/PlanDiffView.tsx`

```tsx
// pages/curator.tsx — 默认 tab 是手动编辑工作台
// 左侧:现有收藏夹结构(可改名 / 删除空夹)
// 右侧:AI 草稿体系(TaxonomyDraftEditor,支持改名/合并/拆分)
// 顶部:打开 AI 助手按钮 → 调 ChatDrawer
// 底部:[生成整理审计报告] 按钮 → POST /api/curator/audit/reorganize
```

- [ ] Step 1:写 pages/curator.tsx 框架
- [ ] Step 2:写 TaxonomyDraftEditor(改名 + 合并)
- [ ] Step 3:写 PlanDiffView(对比前/后)
- [ ] Step 4:`web build` 通过
- [ ] Step 5:commit

---

### Task 11: 前端 — /settings 模型管理

**Files:**
- Modify: `web/src/pages/settings.tsx`

新增「模型管理」区域:
- 选服务商(下拉,Ollama / DeepSeek / 火山方舟 / MiniMax / 自定义)
- 选模型 → 自动填 contextWindow / maxOutput
- 「测试连接」按钮 → POST /api/settings/test-llm(后端 Task 6 已加)
- ⚠️ 测试请求的 params 在后端要脱敏(§6 红队加固)

- [ ] Step 1:写模型选择 UI
- [ ] Step 2:接测试连接 API
- [ ] Step 3:`web build` + typecheck 通过
- [ ] Step 4:commit

---

### Task 12: 集成测试 + 端到端验证

- 后端 `npm test -w server` 全绿(180+ = 现有 + M4 新增)
- 前端 `cd web && npm run typecheck` 绿
- `cd web && npx max build` 绿
- 流式端到端:用 vitest 集成测试 mock 整个 chat 流,验证 chunk → SSE → DB → 历史读取

- [ ] Step 1:补任何缺失的集成测试
- [ ] Step 2:跑全量
- [ ] Step 3:commit(若有测试改动)

---

### Task 13: 截图自查 + 调风格

跟 M3 一样,设计落地完成后做截图自查:

**统一检查清单**:
- AI 助手悬浮图标:所有页面右下角一致显示
- 聊天抽屉:深底 + 青色描边 + 切角(与 §11 一致)
- /curator 工作台:左右对比,体系对比视觉清晰
- /settings 模型管理:HD 风格统一

- [ ] Step 1:UI review 自查(基于代码 + 描述)
- [ ] Step 2:若 M3 有遗漏的调风格小问题,这里一起收尾
- [ ] Step 3:commit

---

## 执行交接

Plan 完成,保存到 `docs/superpowers/plans/2026-09-15-M4-curator.md`。

两种执行方式:

**1. Subagent-Driven(推荐)** — 每个任务派新 subagent,我 review,快
**2. Inline Execution** — 在当前会话跑,checkpoint review

哪种?

---

## 顺序与依赖(粗略)

依赖图:

```
T1(db migration) ← T3,T6(都需要新表)
T2(LLM provider) ← T3(用provider)、T5(用context预算)
T3(chat)        ← T2(provider)、T1(表)
T4(keyword)     ← T5(分类器第1阶段用keyword结果)
T5(classifier)  ← T2、T4
T6(HTTP路由)    ← T3、T5
T7(前端types+api) ← T6
T8(悬浮图标)    ← T7
T9(聊天抽屉)    ← T7、T8
T10(/curator页) ← T7
T11(/settings模型) ← T7
T12(集成测试)    ← T6、T10、T11
T13(截图自查)   ← T12
```

---

## 接下来我会做的(等你 OK)

下一步把每个任务填完整步骤:
- Tasks 文件
- Interfaces(consumes/produces)
- 完整 TDD 步骤(每个 step 5-15 行)
- 准确的 `npm test`/`npm run typecheck` 命令

或者如果你觉得任务切分/命名有问题,现在告诉我,我重排再展开。

---

## Self-Review(待完整步骤填完后做)

待 §1-§13 完整步骤都写完后再跑 self-review:
1. spec 覆盖度(§3 §5 §9 §11 各项实现落在哪个 task)
2. 占位符扫描
3. 接口一致性(T2 的 ModelMeta 在 T5/T11 是否一致引用,etc)
4. M5 边界:M4 不写回 B 站,Plan 里所有"执行写回"都是占位

---

## 风格与不变项

- 配置文件改动(`package.json`、`.umirc.ts`):只追加,不改坏现有
- 测试:LLM provider 必须 mock,不调真实 API
- 所有对 AI 模型的输入输出走 `provider.ts` 一处,便于后续切模型
- 删除保险(§10.1.1):curator 路由**不实现** remove_item 写回路径,
  即使 AI 意外产生也由 plan 校验拦掉

---

## 风格选择(M4 仍沿用)

- HUD 深底 `#0e0e17` + 青色 `#5ef6ff` + 切角 + 等宽数字
- AI 助手用 morphicons 状态形变(默认/激活/工作中)
- 头像位图后续从社区素材选(先做占位组件,character 不写死)
