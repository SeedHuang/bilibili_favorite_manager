# 历史遗留的 npm 配置:`install-links=true` 曾让 npm 对 `file:` 依赖按"复制"安装而非建软链
# (原因与结论见 docs/issues/2026-09-23-shared-package-local-dev.md)。
# 自 2026-09-24 迁移到 pnpm 后,`@seedhuang/ai_suit_tool` 走 registry ^0.1.0,
# 项目内已无 `file:` 依赖,本配置处于休眠状态;且 pnpm 11+ 不从 .npmrc 读取
# registry/auth 以外的设置。
install-links=true
