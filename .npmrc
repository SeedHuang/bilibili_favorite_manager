# `file:` 依赖按"打包安装"（复制）而不是建 junction/软链。
#
# 为什么必须:server 与 web 都用 `"@seedhuang/ai_suit_tool": "file:../../ai_suit_tool"`。
# 默认(file: 走软链接)时,D:\Seed\ai_suit_tool 会原样挂在 node_modules 下 ——
# 而那个仓库**自己也有 node_modules**(react / antd 是它的 devDependency)。
# 于是包里的 `import 'antd'` 会命中它自己的那份 antd,而不是本项目根目录的那份:
#   1. 主题断链 —— antd 的 ConfigProvider 主题走它自己的 React context,第二份实例
#      读不到 web/src/layouts 里 `<ConfigProvider theme={{algorithm: darkAlgorithm}}>`,
#      三卡里的 Select / Input / Button 会退回 antd 默认浅色。
#   2. dev 下 MFSU 预打包会把两份 antd 一起编进 dep bundle(实测两个模块命名空间)。
#
# 打开 install-links 后 npm 按 package.json 的 `files` 字段把包复制进 node_modules,
# 不携带对方的 node_modules —— 包里所有裸导入(react / antd / @ant-design/icons)
# 都逐级向上命中本项目根 node_modules 的同一份,dev / prod 一致。
#
# 代价:改 ai_suit_tool 的源码后,需要在该包 `npm run build` + 在本仓库重新
# `npm install` 才会进 node_modules(复制语义没有"链接"的即时性)。
install-links=true
